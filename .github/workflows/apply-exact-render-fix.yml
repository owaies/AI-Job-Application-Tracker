name: Apply exact render diagnostic

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Add targeted CustomSelect runtime diagnostic
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path('frontend/src/App.tsx')
          s = p.read_text()
          old = "  const active = value || placeholder\n"
          new = '''  const active = value || placeholder\n  const runtimeActive: unknown = active\n  if (typeof runtimeActive !== 'string') {\n    const tag = Object.prototype.toString.call(runtimeActive)\n    const description = typeof runtimeActive === 'function' ? `[Function ${runtimeActive.name || 'anonymous'}]` : String(runtimeActive)\n    throw new Error(`CustomSelect invalid value: label=${String(label)}; type=${typeof runtimeActive}; tag=${tag}; value=${description}`)\n  }\n'''
          count = s.count(old)
          if count != 1:
              raise SystemExit(f'Expected exactly one CustomSelect active assignment, found {count}')
          p.write_text(s.replace(old, new))
          print('Added targeted CustomSelect runtime diagnostic.')
          PY
      - name: Verify and build
        run: |
          grep -n -A6 'const active = value || placeholder' src/App.tsx
          npm install --include=dev
          npm run build
        working-directory: frontend
      - name: Commit diagnostic and remove one-time workflow
        run: |
          rm .github/workflows/apply-exact-render-fix.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add frontend/src/App.tsx .github/workflows/apply-exact-render-fix.yml
          git commit -m 'debug: expose CustomSelect runtime value'
          git push origin HEAD:main
